# Approved Mockup Validation Notes

- The revised coordinator rendered the title **FROST 3-of-5 Monero Escrow Demo** in the completed state.
- Buyer, seller, and mediator cards rendered visible green **FROST backup** controls after setup material was available.
- The completed happy-path mediator card stated that the mediator remained absent.
- The dark **Live protocol terminal** rendered newest first and exposed a **Pause** control.
- The event-stream endpoint returned its initial server-sent `protocol` connection event over a GET request.
- During a fresh buyer-plus-mediator refund run, the already-open page received new mediator, chain, automatic-scan, and refund-completion events at the top of the terminal without a full document reload. The older card summary remained until a normal page navigation, which is the intended no-page-movement behavior for the live terminal.
- The revised dashboard renders the live terminal between buyer and seller cards on a desktop viewport. The supplied frost-themed Monero artwork is visible as a dimmed right-side background, while the workflow text and controls remain readable. Before setup, all three FROST backup controls render as red unavailable buttons.
- In a browser-driven clean-session test, clicking **Initialize 3-of-5 escrow** changed the visible status to `initializing`, replaced the setup button with the current setup stage and cancel control, and inserted setup events into the central terminal without browser navigation or manual refresh.
- Browser inspection confirmed that live protocol entries arrive with role prefixes such as `[buyer]`. The client-side parser was adjusted to preserve its regular-expression escapes through server-side template rendering so those live entries receive the matching role badge rather than a generic system badge.
- After restarting the corrected coordinator, the central terminal rendered historical buyer and seller events with their matching badges. A browser-driven payment action immediately added a new buyer-tagged funding-broadcast event without navigating away from the page.
- In the corrected funded-state design, the approved FROST backup warning renders directly below the session status and above every workflow card. The central terminal retains live newest-first behavior but no longer displays the phrase "Newest event first." Buyer and seller each expose distinct release/refund consent controls, while the mediator remains limited to the separately disclosed dispute controls.
- Route-level validation recorded buyer-only release consent as `{"buyer":"release"}` with `payout: null` and mediator state `not running`. Recording matching seller release consent then produced a real completed `happy-release` settlement: the terminal recorded buyer shares 1–2 plus seller shares 3–4, explicitly confirmed no mediator secret was loaded, and the completed screen labeled the path **Buyer and seller coordinated release**.
- The funded workflow no longer visibly displays the non-live **live session age** or **last protocol event** pills. The buyer card now links its displayed funding transaction ID to **Verify on fakechain**.
- Browser validation opened that verification route for funding transaction `df917769bfa213503492de3d8c1c9581bc68225f74830780c428b812bc5c3508`. It displayed the unmodified local daemon `get_transactions` request and response: fakechain node height 1015, transaction block height 1000, 15 confirmations, `in_pool: false`, and exactly one transaction returned. The page also exposes a download of the exact JSON and a copyable direct loopback `curl` command.
